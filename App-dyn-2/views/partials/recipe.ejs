<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <title> <%= sandwich.title %> </title>
</head>

<body>

  <% include ../partials/header %>

  <h1>🎉<%= sandwich.title %> recipe!🎉</h1>

  <div>
    <ol>
      <% Object.keys(sandwich['ingredients']).forEach(function (id) { %>
      <li><%= sandwich['ingredients'][id] %> </li>
      <% }) %>
    </ol>
  </div>

</body>

</html>
